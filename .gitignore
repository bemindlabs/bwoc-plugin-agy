# BWOC agent worktrees (debase worktreeBase points here)
/worktrees/
.DS_Store
.bwoc/
node_modules/

# Locally-generated skill re-exports (scripts/sync-skills.sh) — never committed.
# Narrow glob so it never matches the committed skills/bwoc-* coordination skills.
/skills/fw-*/
